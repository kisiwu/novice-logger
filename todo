- debug: custom formatter pretty-print single line
- rewrite in ts (typescript) ?

- tests:
  - Logger.write: 
    - should not overwrite "write" from default logger
  - Logger.createLogger:
    - custom "write" function
    - "alwaysFormat=true" to receive formatted line in "write" function

- doc:
  - log levels
  - custom level prefix
  - set logger level

Archive:
- log levels @done
- custom level prefix @done
- set logger level @done
